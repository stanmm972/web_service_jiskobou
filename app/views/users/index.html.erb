<div class="container">
    <div class="row">
        <h1>Users</h1>
        <ul>
            <% @users.each do |user| %>
                <li><%= link_to user.email, user_path(user) %></li>
            <% end %>
        </ul>
    </div>
</div>